build:
  version: 2016-03-14 # version of the build schema.
  steps:
    builder:
      name: builder
      dockerfile: Dockerfile
      secrets:
        RACK_ENV:
          type: env
          value: _env(RACK_ENV)
        RAILS_ENV:
          type: env
          value: _env(RAILS_ENV)
        SECRET_KEY_BASE:
          type: env
          value: _env(SECRET_KEY_BASE)
